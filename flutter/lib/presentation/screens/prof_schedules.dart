import 'package:flutter/material.dart';

class ProfessorScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Професор'),
      ),
      body: Center(
        child: Text('Content for Професор screen'),
      ),
    );
  }
}
