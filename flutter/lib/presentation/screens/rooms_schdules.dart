import 'package:flutter/material.dart';

class RoomScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Простотрија'),
      ),
      body: Center(
        child: Text('Content for Простотрија screen'),
      ),
    );
  }
}
